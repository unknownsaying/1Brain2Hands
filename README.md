not peer-to-peer protocol
      
    we don't use http http, ipv4 ipv6, LAN WLAN, WAN ping ,TCP/IP, div curl, grad nabla, path integral(double crak disturbution), 
    only apply dxdydzdt in Visual Basic .net and draw some simple and pure colored square time tunnel static image
not belongs to anybody on earth, only owner is the earth mother herself 
